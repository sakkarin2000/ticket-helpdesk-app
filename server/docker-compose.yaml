version: '3.9'
services:
  ts-backend-node:
    build:
      context: .
      dockerfile: dockerfile
      target: base
    container_name: ts-backend-node
    ports:
      - '5001:5001'
      - '9229:9229'
    volumes:
      - .:/home/node/app
      - /home/node/app/node_modules
    environment:
      - DATABASE_URL=localhost:5432
      - DBUSER=helpdesk_admin
      - DBPASSWORD=tick@dminP@ss
    command: npm run dev
